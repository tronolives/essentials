const tailwindcss = require('tailwindcss');

module.exports = {
    plugins: [
        require('postcss-preset-env'),
        require('autoprefixer'),
        require('tailwindcss'),
        require('autoprefixer')
    ]
}